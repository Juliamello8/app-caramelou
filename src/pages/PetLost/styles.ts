import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    containerLost: {
        width: '100%',
        height: '100%',
        paddingBottom: 80,
    },
    contentsLost: {
        backgroundColor: '#fff',
        borderRadius: 25,
        display: 'flex',
        alignItems: 'center',
    }
});